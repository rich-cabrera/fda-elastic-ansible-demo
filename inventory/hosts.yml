---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

  children:
    managed_hosts:
      hosts:
        ip-172-31-65-25.ec2.internal:
          ansible_host: ec2-44-197-101-251.compute-1.amazonaws.com
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/Documents/scripts/rcabrera-keypair.pem

    eda_controllers:
      hosts:
        localhost:
          ansible_connection: local
